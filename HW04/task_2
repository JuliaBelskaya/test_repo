# Дан список целых чисел. Подсчитать сколько четных чисел в списке.

lst = list(range(1, 11))
n_even = 0
i = 0
while i < len(lst):
    if lst[i] % 2 == 0: #if not list[i] % 2:
        n_even += 1
    i += 1
print(n_even)


#var2
numbers = [1, 5, 7, -10, -2]
amount = 0
i = 0
numbers_len = len(numbers)
while i < numbers_len:
    if not numbers[i] % 2:
        amount += 1
    i += 1
print(amount)
